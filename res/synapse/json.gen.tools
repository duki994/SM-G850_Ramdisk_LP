#!/sbin/busybox sh

cat << CTAG
{
	name:"Kernel tools"
	elements:[
		{ SButton: {
			label:"Save last_kmsg"
			action:"tools kmsg"
		}},
		{ SButton: {
			label:"Save dmesg"
			action:"tools dmesg"
		}},
		{ SButton: {
			label:"Save logcat"
			action:"tools logcat"
		}}
	]
}